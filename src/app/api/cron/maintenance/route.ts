import {NextResponse} from "next/server";import {createAdminClient} from "@/lib/supabase/admin";import {log} from "@/lib/logger";
export async function GET(request:Request){const expected=process.env.CRON_SECRET;if(!expected||request.headers.get("authorization")!==`Bearer ${expected}`)return NextResponse.json({error:"Unauthorized"},{status:401});const result=await createAdminClient().rpc("run_scheduled_maintenance",{});if(result.error){log("error","scheduled_maintenance_failed",{message:result.error.message});return NextResponse.json({status:"failed"},{status:500});}log("info","scheduled_maintenance_complete",{result:result.data});return NextResponse.json({status:"ok",result:result.data});}
